---
description: Have a routed model critique files, a diff, or a design — ranked issues with concrete fixes
---

Target to critique: $ARGUMENTS

Read the Claude Roundtable skill (`skills/claude-roundtable/SKILL.md`) and run its CRITIQUE
flow on the target above (pass paths, not contents). If the user named a
model, route to that model. After the critique returns, add your own short
take only where you disagree or would reprioritize — then stop.
